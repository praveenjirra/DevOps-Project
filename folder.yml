---
   - hosts: localhost
     #remote_user: root
     become: yes
     sudo: yes
     tasks:
     - name: Create Folder
       file:
        path: /home/ubuntu/tom
        state: directory
        mode: 0755
        owner: ubuntu
        group: ubuntu
