---
   - hosts: localhost
     remote_user: root

     tasks:
     - name: Create Folder
       file:
        path: /home/ubuntu/tom
        state: directory
        mode: 0755
        owner: ubuntu
        group: ubuntu
